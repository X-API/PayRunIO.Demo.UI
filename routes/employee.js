var router = require("koa-router")();

router
    .get("/", (next) => {
        
    })
    .post("/", (next) => {

    })
    .get("/:id", (next) => {
        
    })
    .post("/:id", (next) => {
        
    })
    .post("/:id/delete", (next) => {
        
    });

module.exports = router;